function doGame(){var e=100*Math.random(),t=Math.floor(e);for(target=t+1;!finished;)guess_input_text=prompt("I am thinking of a number in the range 1 to 100. \n\n What is the number?"),guess_input=parseInt(guess_input_text),guesses+=1,finished=checkGuess()}function checkGuess(){return isNaN(guess_input)?(alert("Not a number, try again"),!1):1>guess_input||guess_input>100?(alert("Please enter an integer number in the range 1 to 100."),!1):guess_input>target?(alert("Number is too large"),!1):target>guess_input?(alert("Number is too small"),!1):(alert("You got it! the number was "+target+"!"),!0)}var target,guess_input_text,guess_input,finished=!1,guesses=0;
